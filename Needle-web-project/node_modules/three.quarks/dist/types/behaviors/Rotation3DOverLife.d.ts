import { Behavior } from "./Behavior";
import { Particle } from "../Particle";
import { RotationGenerator } from "../functions/RotationGenerator";
export declare class Rotation3DOverLife implements Behavior {
    angularVelocity: RotationGenerator;
    dynamic: boolean;
    type: string;
    private tempQuat;
    constructor(angularVelocity: RotationGenerator, dynamic: boolean);
    initialize(particle: Particle): void;
    update(particle: Particle, delta: number): void;
    toJSON(): any;
    static fromJSON(json: any): Behavior;
    frameUpdate(delta: number): void;
    clone(): Behavior;
}

import { Document, ILogger, Texture } from '@gltf-transform/core';
import { NEEDLE_compression_texture_schema } from '../extensions';
import type { SquooshLib } from '@gltf-transform/functions/src/types/squoosh-lib';
import { INeedleTextureTransformContext } from './needle_common';
export interface NeedleWebPOptions {
    squoosh: typeof SquooshLib | null;
    jobs: number;
    auto?: boolean;
}
export declare class NeedleWebPContext implements INeedleTextureTransformContext {
    pool: SquooshLib.ImagePool;
    options: NeedleWebPOptions;
    logger: ILogger;
    constructor(options: NeedleWebPOptions);
    prepare(document: Document): Promise<void>;
    process(index: number, texture: Texture, settings: NEEDLE_compression_texture_schema): Promise<boolean>;
    finalize(document: Document): Promise<void>;
}

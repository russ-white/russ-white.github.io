"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const commandLineArgs = require("command-line-args");
const stop_other_vite_server_1 = require("./actions/stop-other-vite-server");
const log_1 = require("./utils/log");
const optionDefinitions = [
    { name: 'directory', type: String, multiple: false, defaultOption: true }
];
const options = commandLineArgs(optionDefinitions);
if (!options.directory || options.directory.length === 0) {
    (0, log_1.error)("Please specify a directory");
    process.exit(1);
}
(0, stop_other_vite_server_1.stopOtherViteServers)(options.directory);

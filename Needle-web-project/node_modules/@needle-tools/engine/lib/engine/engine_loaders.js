import { DRACOLoader } from 'three/examples/jsm/loaders/DRACOLoader.js';
import { KTX2Loader } from 'three/examples/jsm/loaders/KTX2Loader.js';
import { getParam } from "./engine_utils";
const debug = getParam("debugdecoders");
let dracoLoader;
let ktx2Loader;
export function setDracoDecoderPath(path) {
    if (path !== undefined && typeof path === "string") {
        if (!dracoLoader)
            dracoLoader = new DRACOLoader();
        if (debug)
            console.log("Setting draco decoder path to", path);
        dracoLoader.setDecoderPath(path);
    }
}
export function setDracoDecoderType(type) {
    if (type !== undefined && typeof type === "string") {
        if (!dracoLoader)
            dracoLoader = new DRACOLoader();
        if (debug)
            console.log("Setting draco decoder type to", type);
        dracoLoader.setDecoderConfig({ type: type });
    }
}
export function setKtx2TranscoderPath(path) {
    if (path !== undefined && typeof path === "string") {
        if (!ktx2Loader)
            ktx2Loader = new KTX2Loader();
        if (debug)
            console.log("Setting ktx2 transcoder path to", path);
        ktx2Loader.setTranscoderPath(path);
    }
}
export function addDracoAndKTX2Loaders(loader, context) {
    if (!dracoLoader) {
        dracoLoader = new DRACOLoader();
        dracoLoader.setDecoderPath('./include/draco/');
        dracoLoader.setDecoderConfig({ type: 'js' });
        if (debug)
            console.log("Setting draco decoder path to", './include/draco/');
    }
    if (!ktx2Loader) {
        ktx2Loader = new KTX2Loader();
        ktx2Loader.setTranscoderPath('./include/ktx2/');
        if (debug)
            console.log("Setting ktx2 transcoder path to", './include/ktx2/');
        if (context.renderer)
            ktx2Loader.detectSupport(context.renderer);
    }
    loader.setDRACOLoader(dracoLoader);
    loader.setKTX2Loader(ktx2Loader);
}
//# sourceMappingURL=engine_loaders.js.map
import { BufferGeometry, Vector3 } from 'three';
import { ParticleSystemBatch, ParticleSystemBatchSettings } from "./ParticleSystemBatch";
export declare class TrailBatch extends ParticleSystemBatch {
    geometry: BufferGeometry;
    private positionBuffer;
    private previousBuffer;
    private nextBuffer;
    private uvBuffer;
    private sideBuffer;
    private widthBuffer;
    private colorBuffer;
    private indexBuffer;
    constructor(settings: ParticleSystemBatchSettings);
    setupBuffers(): void;
    expandBuffers(target: number): void;
    rebuildMaterial(): void;
    vector_: Vector3;
    update(): void;
    dispose(): void;
}

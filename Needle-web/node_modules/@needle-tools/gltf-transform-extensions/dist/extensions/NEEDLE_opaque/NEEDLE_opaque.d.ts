import { WriterContext, PropertyType } from '@gltf-transform/core';
import { IExtensibleProperty } from '@gltf-transform/core/dist/properties';
export declare type Options = {
    debug?: boolean;
    write?: boolean;
};
export interface IOpaqueExtension {
}
export interface IOpaqueProperty {
    /** used by progressive texture extension to pass an extension between documents */
    getExtensionDefinition(): object;
}
export interface IExtensionWriter {
    /** used by progressive texture extension to pass an extension between documents */
    assignTo?(obj: object): any;
    write(context: WriterContext, prop: IExtensibleProperty): void;
}
export declare function createOpaqueExtension(name: string, types: PropertyType | PropertyType[], opts?: Options): IOpaqueExtension;
